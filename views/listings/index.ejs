<%layout("/layouts/boilerplate")-%>
    <h1 style="color: #fe424d">WanderLust</h1>
    <div class="row row-cols-lg-3 row-cols-md-2 row-cols-sm-1 g-3 my-3"> 
        <% for(let listings of allListings){%>
        <a href="/listings/<%=listings._id%>" class="listing-link">
            <div class="card col listing-card" >
            <img src="<%=listings.image.url%>" class="card-img-top" alt="listing_image" style="height: 20rem;">
            <div class="card-img-overlay">WanderLust</div>  
                <div class="card-body">
                    <p class="card-text">
                        <b><%= listings.title%> <br></b>
                        
                        <% if (listings.price) { %>
                            &#8377;<%= listings.price.toLocaleString("en-IN") %> /night
                            <% } else { %>
                            <span class="text-muted">Price not available</span>
                        <% } %>

                    </p>
                </div>
            </div>
        </a>
    <%}%>
</div>


